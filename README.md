# Stream

<pre>


  stream()
  filter()
  map()
  collect()
  count()
  sorted()
  sorted(Comparator)
  min(Comparator)
  max(Comparator)
  forEach()
  toArray()
  Stream.of()
  
  Convert array to stream.
 

<code>
